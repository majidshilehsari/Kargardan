# 🤝 API ایجنت همکار — کارگردان

> این فایل را به ایجنت همکارت بده. هرچه لازم دارد اینجاست.
> نسخه: ۰.۴.۰ · آخرین به‌روزرسانی: ۲۷ شهریور ۱۴۰۵ (2026-09-18)

---

## ایجنت همکار کیست؟

کارگردان دو ایجنت دارد:

| ایجنت | دسترسی | کارش |
|---|---|---|
| **ایجنت همکار** | فقط از راه همین API — **بدون** دسترسی به سورس‌کد | داده را می‌خواند و **پیشنهاد** می‌دهد |
| **ایجنت مادر** | سورس‌کد + دیتابیس | برنامه را می‌سازد و توسعه می‌دهد |

⚠️ **مهم‌ترین قاعده:** ایجنت همکار هرگز مستقیم چیزی را عوض نمی‌کند. فقط پیشنهاد می‌گذارد؛
کارفرما خودش تأیید یا رد می‌کند. حتی بعد از تأیید هم قابل برگرداندن است.

---

## راه‌اندازی (کارفرما انجام می‌دهد)

۱. در **Vercel → Settings → Environment Variables** یک متغیر بساز:

   | نام | مقدار |
   |---|---|
   | `AGENT_API_KEY` | یک رشتهٔ تصادفی طولانی (مثلاً ۴۰ کاراکتر) |

۲. برنامه را دوباره Deploy کن.
۳. کلید را به ایجنت همکار بده.

🔐 **اگر این متغیر تنظیم نشده باشد، همهٔ مسیرهای `/api/agent/*` بسته‌اند** و به‌جای داده،
راهنمای تنظیم برمی‌گردانند. پس به‌صورت پیش‌فرض امن است.

> 💡 می‌توانی کلید را از تب **«🔌 ایجنت همکار»** در خود برنامه هم ببینی و متن آماده را کپی کنی.

---

## احراز هویت

همهٔ درخواست‌ها این هدر را لازم دارند:

```
Authorization: Bearer <AGENT_API_KEY>
```

(یا `x-agent-key: <AGENT_API_KEY>`)

بدون کلید درست: `401` · اگر کلید تنظیم نشده باشد: `503`

---

## ۱) خواندن راهنما

```http
GET /api/agent/context
Authorization: Bearer <AGENT_API_KEY>
```

برمی‌گرداند: هدف برنامه، فلسفه، مدل داده، انواع پیشنهاد مجاز با ساختار `payload`، قواعد،
و خلاصهٔ وضعیت فعلی. **همیشه اول این را بخوان.**

---

## ۲) خواندن وضعیت کامل دیتابیس

```http
GET /api/agent/state
Authorization: Bearer <AGENT_API_KEY>
```

پاسخ شامل:

| بخش | توضیح |
|---|---|
| `counts` | شمارش کارها، پروژه‌ها، صندوق ذهن، سطل بازیافت، پیشنهادها |
| `projects` | پروژه‌ها + `nextStep` (قدم بعدی هر جبهه) + تعداد کارهای باز |
| `tasks` | کارهای باز + `projectName` + `ageDays` (چند روز است مانده) |
| `doneTasks` | کارهای تمام‌شده |
| `inbox` | صندوق ذهن + `ageDays` |
| `trash` | 🗑 سطل بازیافت — چیزی که در این برنامه پاک نمی‌شود |
| `suggestions` | پیشنهادهای قبلی و وضعیتشان |
| `recentHistory` | تاریخچهٔ تغییرات |

🔒 این مسیر **فقط خواندنی** است.

---

## ۳) خواندن پیشنهادهای قبلی

```http
GET /api/agent/suggestions?status=pending
Authorization: Bearer <AGENT_API_KEY>
```

**قبل از دادن پیشنهاد جدید، این را بزن تا پیشنهاد تکراری ندهی.**

---

## ۴) ثبت پیشنهاد جدید

```http
POST /api/agent/suggestions
Authorization: Bearer <AGENT_API_KEY>
Content-Type: application/json
```

### بدنه

```json
{
  "agentName": "ایجنت همکار",
  "title": "عنوان کوتاه و روشن",
  "body": "چرا این پیشنهاد را می‌دهم — این را جدی بگیر",
  "kind": "task_set_status",
  "payload": { "id": "<شناسهٔ کار>", "status": "later" }
}
```

### انواع پیشنهاد و ساختار payload

| `kind` | کارش | payload |
|---|---|---|
| `task_add` | ساخت کار جدید | `{ text* , status?, projectId?, waitingOn?, note? }` |
| `task_update` | ویرایش کار موجود | `{ id*, text?, waitingOn?, note?, projectId? }` |
| `task_set_status` | تغییر وضعیت کار | `{ id*, status* }` |
| `task_toggle_done` | تمام‌کردن / برگرداندن | `{ id*, done? }` |
| `project_add` | ساخت پروژهٔ جدید | `{ name*, status? }` |
| `project_set_status` | تغییر وضعیت پروژه | `{ id*, status* }` |
| `note` | یادداشت آزاد (بدون تغییر داده) | لازم نیست |

`*` = اجباری

**مقادیر مجاز:**
- وضعیت کار: `now` (الان) · `later` (بعداً) · `waiting` (منتظر دیگری) · `parked` (پارک‌شده)
- وضعیت پروژه: `active` (فعال) · `queued` (در صف) · `parked` · `done`

### ارسال گروهی

می‌توانی چند پیشنهاد را یک‌جا بفرستی (حداکثر ۵۰):

```json
{ "suggestions": [ { "title": "...", "kind": "..." }, { "title": "...", "kind": "..." } ] }
```

### پاسخ

```json
{
  "ok": true,
  "createdCount": 1,
  "rejectedCount": 0,
  "created": [ { "id": "...", "status": "pending", ... } ],
  "rejected": [],
  "message": "پیشنهادها ثبت شدند و در تب «پیشنهادات ایجنت» منتظر تأیید کارفرما هستند."
}
```

⚠️ `status: "pending"` یعنی **هنوز اجرا نشده**. کارفرما باید دستی تأیید کند.

---

## نمونهٔ curl

```bash
export BASE="https://your-app.vercel.app"
export AGENT_API_KEY="کلید خودت"

# ۱) راهنما
curl -s -H "Authorization: Bearer $AGENT_API_KEY" "$BASE/api/agent/context"

# ۲) وضعیت کامل
curl -s -H "Authorization: Bearer $AGENT_API_KEY" "$BASE/api/agent/state"

# ۳) ثبت پیشنهاد
curl -s -X POST "$BASE/api/agent/suggestions" \
  -H "Authorization: Bearer $AGENT_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "انتقال «تماس با بانک» به بعداً",
    "body": "۹ روز است در الان مانده و قدم بعدی هیچ جبهه‌ای نیست.",
    "kind": "task_set_status",
    "payload": { "id": "t2", "status": "later" }
  }'
```

---

## مدل داده (خلاصه)

### کار (task)
| فیلد | نوع | توضیح |
|---|---|---|
| `id` | string | شناسه |
| `text` | string | متن کار |
| `status` | enum | `now` · `later` · `waiting` · `parked` |
| `projectId` | string \| null | پروژه‌ی مربوطه |
| `isNextStep` | boolean | قدم بعدیِ آن جبهه (برای هر پروژه حداکثر یکی) |
| `waitingOn` | string | فقط برای `waiting`: توپ در زمین کیست |
| `note` | string | فقط برای `parked`: چرا پارک شده |
| `done` | boolean | انجام‌شده؟ |
| `createdAt` / `updatedAt` | number | میلی‌ثانیهٔ unix |
| `ageDays` | number | (فقط در خروجی API) چند روز از ساختش می‌گذرد |

### پروژه (project)
| فیلد | نوع |
|---|---|
| `id` | string |
| `name` | string |
| `status` | `active` · `queued` · `parked` · `done` |
| `nextStep` | string \| null (فقط در خروجی API) |
| `openTaskCount` | number (فقط در خروجی API) |

### صندوق ذهن (inboxItem)
`{ id, text, createdAt, ageDays }`

---

## فلسفه‌ای که پیشنهادهایت باید با آن هم‌راستا باشد

کارگردان قرار نیست زندگی را بدون استرس کند؛ قرار است **نگذارد استرس، مدیر زندگی شود.**

| اصل | یعنی چه برای پیشنهادهای تو |
|---|---|
| **۱. تخلیهٔ کامل** | هر چیزی که ذهن را اشغال کرده باید جایی بیرون از ذهن زندگی کند. صندوق ذهنِ پرشده بد نیست؛ صندوق ذهنِ پردازش‌نشده بد است. |
| **۲. تفکیک مهم از فوری** | اگر همه‌چیز «الان» است، یعنی هیچ‌چیز «الان» نیست. |
| **۳. سقف جبهه‌های فعال** | سقف پیش‌فرض ۳ جبههٔ فعال هم‌زمان. پیشنهاد فعال‌کردن بیشتر را با احتیاط بده. |
| **«پارک‌شده» خوب است** | چیزی که آدم را خسته می‌کند کارهای زیاد نیست؛ **تصمیم‌های باز** است. پارک‌کردن یعنی تصمیم بسته شده. |
| **قدم بعدی** | هر جبههٔ فعال باید دقیقاً یک قدم بعدی داشته باشد — نه بیشتر، نه کمتر. |

---

## قواعد طلایی برای ایجنت همکار

۱. **کوچک و مشخص.** هر پیشنهاد = یک تغییر. «بهبود کل سیستم» پیشنهاد نیست.
۲. **همیشه `body` بنویس.** چرا این پیشنهاد را می‌دهی؟ بدون دلیل، کارفرما تصمیم نمی‌گیرد.
۳. **قبل از پیشنهاد، `GET /api/agent/suggestions` را بزن.** تکراری نده.
۴. **پیشنهاد زیاد نده.** ۵ پیشنهاد خوب بهتر از ۵۰ پیشنهاد پرت است.
۵. **از `ageDays` استفاده کن.** کاری که ۳۰ روز در «الان» مانده، احتمالاً یا «بعداً» است یا باید پارک شود.
۶. **پروژه‌ی فعال اضافه پیشنهاد نکن** — سقف ۳ است.
۷. **حذف پیشنهاد نده.** در این برنامه «حذف» وجود ندارد، فقط «پارک‌شده». کارگردان چیزی را پاک نمی‌کند.
۸. **`note` را فراموش نکن.** اگر چیز مفیدی می‌بینی ولی نمی‌دانی چه تغییری لازم است، از نوع `note` استفاده کن.

---

## 🗑 دربارهٔ سطل بازیافت

هیچ چیزی در کارگردان پاک نمی‌شود. هر «حذف» یعنی رفتن به سطل بازیافت، و هر چیزی از آنجا
برمی‌گردد. حتی پیشنهادهایی که ایجنت داده و کارفرما تأیید کرده، از تب «پیشنهادات ایجنت»
قابل برگرداندن‌اند.

پس **پیشنهاد دادن ریسک ندارد** — کارفرما همیشه راه برگشت دارد.

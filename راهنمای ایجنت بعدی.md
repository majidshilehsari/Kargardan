# 🧭 راهنمای ایجنت بعدی — پروژه‌ی «کارگردان»

> آخرین به‌روزرسانی: **جمعه ۲۷ شهریور ۱۴۰۵** (۲۰۲۶-۰۹-۱۸)
> آخرین شاخه: `arena/01a0b3ae-kargardan` · آخرین کامیت‌ها: `f0c94d3` (۰.۲.۰) → `220009c`/`0c1a2ce` (۰.۳.۰) → `58b7614`/`72a95d4` (۰.۴.۰) → `19ecd7c`/`5821f15` (۰.۵.۰) → کامیت ۰.۶.۰ (بازچینی هدر + نشانگر بارگذاری) → `741cfaf` (۰.۷.۰ — `?k=` برای ChatGPT + اسپک OpenAPI + بستن دو حفرهٔ امنیتی) → کامیت ۰.۸.۰ (🧠 نقشهٔ ذهنی). جزئیات در `CHANGELOG.md`

---

## ۰) سه قانون طلایی (قبل از هر کاری بخوان)

1. **به `main` دست نزن.** فقط روی شاخه‌ی `arena/...` کار کن. `main` متعلق به کارفرماست.
2. **فقط یک بار پوش کن**، آن هم بعد از تمام‌شدن همه‌ی تغییرات. با `gh` پول‌ریکوئست باز کن، ولی merge را انجام نده.
3. **قبل از هر تغییر در دیتابیس یا بک‌اند، اجازه بگیر** (پروتکل «وضعیت اضطراری» — بخش ۶ همین فایل).

---

## ۱) این برنامه چیست؟

**کارگردان** (نه «کاردان» — این غلطِ املایی رایج است) یک **سیستم‌عامل زندگی شخصی** است:
دلمشغولی‌ها و اهداف و برنامه‌های کاربر را از ذهنش بیرون می‌کشد، مرتب می‌چیند و یک نمای کامل از زندگی می‌سازد. هم دستیار شخصی است، هم سیستم مدیریت زندگی.

سه لایه‌ی پیاده‌شده تا امروز:

| لایه | چیست | فایل |
|---|---|---|
| ۱. تخلیه‌ی کامل ذهن | هر چیزی که ذهن را اشغال کرده بیرون می‌آید | `components/Inbox.tsx` |
| ۲. تفکیک مهم از فوری | چهار وضعیت: **الان / بعداً / منتظر / پارک‌شده** | `components/TasksBoard.tsx` |
| ۳. سقف جبهه‌های فعال | حداکثر ۳ پروژه‌ی فعال هم‌زمان + صف انتظار | `components/Projects.tsx` |

---

## ۲) وضعیت فعلی کد (اسنپ‌شات)

```
app/
  layout.tsx        ← متادیتا (title: «کارگردان | سیستم‌عامل زندگی») + اسکریپت تم
  page.tsx          ← StoreProvider + App
  icon.svg          ← آیکون: تختهٔ کلاق کلاقی (clapperboard) — نماد «کارگردان»
  globals.css       ← تم روشن/تاریک، RTL، فونت وزیرمتن
components/
  App.tsx           ← پوسته، تب‌ها، تاگل تم
  Dashboard.tsx · Inbox.tsx · TasksBoard.tsx · Projects.tsx · Settings.tsx · common.tsx
lib/
  types.ts          ← مدل داده: Task / Project / InboxItem / AppState (version: 1)
  store.tsx         ← reducer + Context + ذخیره‌ی خودکار در localStorage
  seed.ts           ← داده‌ی نمونه
  format.ts         ← اعداد و تاریخ فارسی
  db-types.ts       ← تایپ‌های وضعیت دیتابیس (بدون وابستگی — امن برای مرورگر)
  db.ts             ← ⚠️ سرور فقط: استخر اتصال pg + getAppPool() + بررسی سلامت
  schema.ts         ← ⚠️ سرور فقط: DDL جدول‌ها — همه با IF NOT EXISTS، هیچ DROP ای نیست
  repo.ts           ← ⚠️ سرور فقط: لایهٔ داده. حذف = پر کردن deleted_at (نه DELETE)
  agent-auth.ts     ← 🔒 کلید خودساخته در دیتابیس + کلید توقف (دیگر متغیر محیطی لازم نیست)
  api-utils.ts      ← کمک‌های مشترک API (پیام‌های فارسی، no-store، resolvePublicOrigin، گارد CSRF)
app/api/
  db/health/        ← وضعیت اتصال دیتابیس (فقط خواندنی)
  migrate/          ← GET: فهرست جدول‌ها · POST: ساخت جدول‌ها (idempotent)
  state/            ← GET/POST وضعیت کامل اپ (نوشتن شرطی با revision)
  trash/            ← 🗑 GET فهرست · POST restore/purge
  suggestions/      ← GET فهرست · [id]/ POST approve/reject/revert
  history/          ← GET تاریخچه
  agent/key/        ← 🔑 کنترل از داخل برنامه: ensure / regenerate / enable / disable
  agent/status/     ← آیا درِ ایجنت باز است؟ (بدون کلید)
  agent/context/    ← 🔒 راهنمای ایجنت
  agent/state/      ← 🔒 خواندن کل دیتابیس
  agent/suggestions/← 🔒 GET/POST پیشنهادها
  bridge/openapi/   ← 📄 اسپک OpenAPI 3.1 (عمومی، بدون کلید) — برای Import در ChatGPT
components/
  Suggestions.tsx   ← تب «پیشنهادات ایجنت» (قبل از داشبورد)
  AgentPanel.tsx    ← تب «ایجنت همکار» (API + متن آماده برای دادن به ایجنت)
  Charts.tsx        ← تب نمودارها (SVG خالص — بدون کتابخانهٔ اضافه)
  MindMap.tsx       ← 🧠 نقشهٔ ذهنی: «من» مرکز، کارها/پروژه‌ها/صندوق دور تا دور (خالص و تست‌پذیر)
  RecycleBin.tsx    ← تب سطل بازیافت
  DatabaseStatus.tsx← کارت وضعیت دیتابیس + دکمهٔ ساخت جدول‌ها
  ChatGptBridge.tsx ← پنل «اتصال ChatGPT» داخل تب ایجنت همکار (نشانی OpenAPI، گام‌ها، کلید)
API-ایجنت-همکار.md  ← مستندات کامل برای ایجنت همکار
.env.example        ← نمونهٔ متغیرهای محیطی (شامل AGENT_API_KEY)
next.config.mjs     ← بدون output: 'export' (عمداً برداشته شد — دلیلش پایین)
```

### ساختار ناوبری (از ۰.۶.۰) — دو سطحی
```
┌─ هدر:  [لوگو] کارگردان   ·  داشبورد · ایجنت همکار · نمودارها · سطل بازیافت · تنظیمات  · 🌙
├─ نوار چسبان (sticky):  🤝 پیشنهادات ایجنت | 🧠 صندوق ذهن | 📋 کارها | 🚀 پروژه‌ها
└─ محتوا
```
- **`PRIMARY_TABS`** در `components/App.tsx` — چهار تب اصلی، در نوار چسبان زیر هدر.
  کارفرما گفته این چهار تا باید همیشه دم دست باشند. **ترتیبش را عوض نکن مگر بگوید.**
- **`SECONDARY_TABS`** — بقیه، داخل هدر (`components/App.tsx` → `<nav className="header-nav">`).
- ⚠️ **tagline («نه کار بیشتر؛ تسلط بیشتر») عمداً حذف شد** — کارفرما گفت حذفش کن.
  برنگردانش.
- ⚠️ **نشانگر «دیتابیس وصل است» از هدر حذف شد.** وضعیت اتصال فقط در
  «تنظیمات → وضعیت دیتابیس» است.
  **ولی `SyncNotice` حذف نشد:** حالا فقط وقتی چیزی خراب است نشان داده می‌شود
  (`syncError` یا `source === 'local'`). اگر کاملاً حذفش کنی، کاربر نمی‌فهمد داده‌اش
  دارد در مرورگر می‌ماند نه دیتابیس — **این یک ریسک از دست رفتن داده است.**

### ذخیره‌سازی داده (مهم!)
- منبع داده **فعلاً** `localStorage` مرورگر است با کلید `kargardan-state-v1`.
- **کلیدهای localStorage را عوض نکن.** «kargardan» همان آوانگاری درست «کارگردان» است؛ تغییرش باعث **از دست رفتن کل داده‌ی کاربر** می‌شود.
- `@fontsource-variable/vazirmatn` باندل‌شده است؛ فونت از CDN نمی‌آید (به Vercel بدون تنظیمات خاص دیپلوی می‌شود).

---

## ۳) 🔴 یافته‌های مهمی که وقت زیادی از من گرفت — تو دیگر دنبالشان نگرد

### ۳.۱. متغیرهای محیطی دیتابیس در سندباکس **قابل مشاهده نیستند**
کاربر گفته `PRISMA_DATABASE_URL` و `POSTGRES_URL` را «به متغیرهای محیطی اضافه کردم»، ولی در سندباکس ایجنت فقط این‌ها وجود دارد:
```
E2B_EVENTS_ADDRESS · E2B_SANDBOX · E2B_SANDBOX_ID · E2B_TEMPLATE_ID
GH_TOKEN · GITHUB_TOKEN · HOME · PATH · PWD · SHELL · …
```
هیچ کلید `POSTGRES_URL` / `PRISMA_DATABASE_URL` / `DATABASE_URL` در محیط ایجنت نیست.
**نتیجه:** ایجنت نمی‌تواند اتصال به دیتابیس را واقعاً تست کند. کاربر باید متغیرها را در **Vercel → Settings → Environment Variables** بگذارد (برای Production/Preview/Development) و برای اجرای محلی در فایل `.env.local`.

### ۳.۲. `output: 'export'` برداشته شد (با تأیید کاربر — عدد ۹۱۳۳۷)
`next.config.mjs` دیگر خروجی استاتیک نمی‌دهد، چون Next.js در حالت `export` هر API Route را با خطای بیلد رد می‌کند. حالا `/api/db/health` یک روت سروری است (`runtime = 'nodejs'`).
**⚠️ پیامد مهم:** برنامه دیگر «بدون سرور» نیست. روی Vercel هیچ تنظیم اضافه‌ای لازم نیست و مثل قبل با یک Deploy بالا می‌آید، ولی دیگر نمی‌شود خروجی را روی هاست استاتیک ساده (مثل GitHub Pages) گذاشت.

### ۳.۳. درایور انتخابی: `pg` (نه Prisma)
با وجود اینکه نام متغیر `PRISMA_DATABASE_URL` است، **Prisma نصب نشده** و نیازی هم نیست. `pg@8` نصب است (`pg` + `@types/pg`). دلیل: Prisma به `prisma generate`، فایل schema و یک مرحلهٔ بیلد اضافه نیاز دارد و برای این حجم پروژه سبک‌تر است. اگر روزی Prisma خواستی، اول **پروتکل بخش ۶** را اجرا کن.

### ۳.۴. برای تست واقعی، یک PostgreSQL موقت بالا آوردم
شبکهٔ `apt` بسته است، ولی پکیج npm **`embedded-postgres`** کار می‌کند و یک PostgreSQL 18.4 واقعی دانلود می‌کند:
```bash
mkdir -p /tmp/pgtest && cd /tmp/pgtest && npm init -y && npm install embedded-postgres
# سپس یک سرور روی پورت 55432 بالا بیاور و در .env.local بگذار:
# PRISMA_DATABASE_URL="postgresql://kargardan:test@127.0.0.1:55432/kargardan"
```
این تنها راهی است که در این سندباکس می‌شود کد اتصال را **واقعاً** تست کرد (نه فقط بیلد). مرورگر headless **کار نمی‌کند** (دانلود Chrome مسدود است)، پس برای بررسی UI از `renderToStaticMarkup` استفاده کن.

### ۳.۵. معماری داده — مهم‌ترین چیزی که باید بفهمی
`lib/store.tsx` حالا دو منبع دارد:
- **دیتابیس** (`source === 'db'`) — منبع اصلی. خواندن از `/api/state`، نوشتن با تأخیر ۷۰۰ms.
- **localStorage** — همیشه نوشته می‌شود (پشتیبان)، و اگر دیتابیس نبود **جایگزین** می‌شود.

**🛡 نوشتن شرطی (optimistic concurrency):** کلاینت شمارهٔ `revision` را نگه می‌دارد و با هر
نوشتن می‌فرستد. `writeState` چک می‌کند؛ اگر دیتابیس جلوتر رفته باشد، **هیچ چیزی نمی‌نویسد**
و `stale: true` برمی‌گرداند. این ویژگی جلوی گم‌شدن داده در نوشتن‌های هم‌زمان را می‌گیرد.
**آن را حذف نکن.**

### ۳.۶. 🗑 حذف نرم — قاعدهٔ تغییرناپذیر
هیچ جای کد نباید `DELETE` روی داده بزند. الگوی درست:
```sql
update <table> set deleted_at = $1, deleted_by = 'user' where ...
```
تنها استثنا: `purgeTrash()` در `lib/repo.ts` که فقط با کلیک صریح کاربر روی «پاک‌کردن قطعی»
صدا زده می‌شود. اگر ایجنت بعدی خواست جایی `DELETE` اضافه کند، **اول پروتکل بخش ۶ را اجرا کند.**

### ۳.۷. ایجنت همکار جدا از ایجنت مادر است
- **ایجنت همکار:** فقط از راه `/api/agent/*`. به سورس‌کد دسترسی ندارد.
- 🔑 **کلید در دیتابیس است، نه در متغیر محیطی.** کارفرما صریحاً گفت نمی‌خواهد متغیر محیطی
  بسازد. کلید در جدول `kargardan_settings` با کلیدهای `agent_api_key` و
  `agent_access_enabled` ذخیره می‌شود. **هیچ تغییر ساختاری در جدول‌ها لازم نشد.**
- 🔴 **کلید توقف:** `setAgentEnabled(pool, false)` ⇒ همهٔ `/api/agent/*` با کد **۴۰۳** رد
  می‌شوند، **حتی با کلید کاملاً درست**. این تست شده و باید حفظ شود.
- کنترل‌ها از `app/api/agent/key/route.ts` با چهار اکشن: `ensure` / `regenerate` /
  `enable` / `disable`. این مسیر خودش با کلید محافظت نمی‌شود (از داخل برنامه صدا زده می‌شود).
- ⚠️ `checkAgentAuth` حالا **async** است و `pool` می‌گیرد. اگر مسیر ایجنت جدیدی اضافه کردی،
  همین امضا را رعایت کن.
- **ایجنت مادر:** تو. سورس‌کد + دیتابیس.
- پیشنهادهای ایجنت **هرگز خودکار اجرا نمی‌شوند**؛ در تب «پیشنهادات ایجنت» می‌نشینند.
- هنگام تأیید، `applySuggestion()` «وضعیت قبل» را در `kargardan_history` ثبت می‌کند
  تا `revertSuggestion()` بتواند برگرداند. **این ثبت قبل از تغییر را حذف نکن.**
- مستندات کامل برای ایجنت همکار: `API-ایجنت-همکار.md`

### ۳.۸. نشانگر «کاری در جریان است» (`lib/busy.ts`)
کارفرما گفت: «وقتی روی تأیید پیشنهاد می‌زنم، طول می‌کشد و کاربر بی‌خبر می‌ماند.»
راه‌حل: یک شمارندهٔ مشترک با `useSyncExternalStore`.

```tsx
import { withBusy } from '@/lib/busy';

await withBusy(async () => {
  const res = await fetch('/api/...', { method: 'POST' });
  // ...
});
```

سه سطح بازخورد:
۱. `<GlobalProgress />` — نوار نازک بالای صفحه (در `App.tsx`، همه‌جا دیده می‌شود).
۲. `<BusyRow text="..." />` — ردیف چرخان داخل کارت.
۳. `<Spinner />` داخل دکمه + کلاس `is-busy` روی کارت (محو و غیرقابل‌کلیک).

**هر عملیات شبکه‌ای جدید را داخل `withBusy` بگذار** — وگرنه کاربر بی‌خبر می‌ماند.

### ۳.۹. `.env.local` فعلی سندباکس است
فایل `.env.local` (در `.gitignore`) به همان PostgreSQL موقت اشاره می‌کند و **فقط داخل سشن ایجنت** معنی دارد. کامیت نمی‌شود. اگر پیش‌نمایش «قرمز» شد یا سشن تمام شد، این طبیعی است — آدرس واقعی کاربر در Vercel است.

### ۳.۱۰. وضعیت گیت‌هاب (بررسی‌شده)
- `origin/main` روی کامیت `f41e08e` است — یک **merge commit** از PR #1 (شاخه‌ی `arena/01a03a2d-kargardan`).
- محتوای `origin/main` با کامیت پایه‌ی این شاخه **کاملاً یکسان** است (`git diff` خالی بود) ⇒ هیچ خطر از دست رفتن کدی وجود ندارد.
- اتصال `gh` فعال است (کاربر `arena-ai-coding-agent[bot]`).

---

### ۳.۱۱. 🤖 اتصال ChatGPT — کلید از پارامتر آدرس (نسخهٔ ۰.۷.۰)

**مسئله:** ChatGPT Actions نمی‌تواند هدر `Authorization` سفارشی بفرستد. راه‌حلِ پیشنهادیِ
ایجنت همکارِ کاربر «ساخت یک سرور واسط (bridge/proxy)» بود که **عمداً رد شد** — چون یک لایهٔ
حمله و یک نقطهٔ خرابی اضافه می‌ساخت. به‌جایش، خودِ کارگردان کلید را از پارامتر آدرس می‌پذیرد:

```ts
// lib/agent-auth.ts — سه منبع، به یک شکل
Authorization: Bearer <key>  |  x-agent-key: <key>  |  ?k=<key> (یا ?key=)
```

و یک **فایل OpenAPI خودتولید** در `GET /api/bridge/openapi` (عمومی — فقط شکل API را
توصیف می‌کند، هیچ داده‌ای نمی‌دهد) که دقیقاً ۴ عملیات ایجنت را دارد. نشانی سرور در سند
از `resolvePublicOrigin(req)` می‌آید تا روی Vercel خودکار درست باشد.

**گام‌های کارفرما:** ChatGPT → Configure → Actions → Import from URL →
`https://<دامنه>/api/bridge/openapi` → Authentication: **API Key / Query / نام `k`** → Save.

⚠️ **محدودیت شناخته‌شده:** در این روش کلید در آدرس دیده می‌شود (لاگ سرور، تاریخچه).
گزینهٔ جایگزین اگر کارفرما خواست: هدر سفارشی با تغییر `operationId`… ولی ChatGPT فعلاً
خط‌مشی Query را ساده‌تر می‌پذیرد. کارفرما هر وقت خواست با «🔄 ساخت کلید جدید» کلید را عوض می‌کند.

### ۳.۱۲. 🔴 حفرهٔ امنیتی روت‌های UI — تصمیم کارفرما لازم است

روت‌های UI (`/api/state`, `/api/trash`, `/api/migrate`, `/api/agent/key`) **احراز هویت ندارند**.
هیچ‌کدام کلید نمی‌خواهند، چون مرورگرِ خود کارفرما باید بتواند بدون کلید کار کند.
نتیجه (تست‌شده): `GET /api/state` بدون هیچ کلید ⇒ ۲۰۰ + کل داده؛ و `POST /api/state`
با `{"revision": -1}` ⇒ بازنویسی کامل دیتابیس (نوع حذف «نرم» بود و برگشت، ولی حفره واقعی بود).

**در ۰.۷.۰ دو ترمیم انجام شد (نه راه‌حل کامل):**
1. در `writeState` مسیر `revision < 0` فقط روی دیتابیس **کاملاً خالی** مجاز است (انتقال اولیه).
2. گارد CSRF با `Sec-Fetch-Site: cross-site` روی POST چهار روت ⇒ حملهٔ از سایت دیگر ۴۰۳ می‌گیرد.

🔴 **باقی‌مانده:** گارد CSRF جلوی `curl`/اسکریپت مستقیم را نمی‌گیرد (چون هدر نمی‌فرستد).
**راه‌حل واقعی = احراز هویتِ روت‌های UI** (مثلاً یک توکن دسترسی که مرورگر یک بار وارد می‌کند
و در localStorage نگه می‌دارد). این تغییر رفتار برنامه است و **بدون اجازهٔ صریح کارفرما انجامش نده.**
جزئیات کامل در گزارش تحویل ۰.۷.۰.

---

### ۳.۱۳. 🧠 نقشهٔ ذهنی (نسخهٔ ۰.۸.۰) — مهم‌ترین نمودار کارفرما

کارفرما صریحاً گفت: **«مهم‌ترین نموداری که برایم مهم است، یک نمودار دایره‌ای است که
همهٔ کارهایی که سرم ریخته را دور تا دور دایره نشان بدهد — خودم وسطش باشم.»**
پس این نمودار با هیچ‌چیز قابل معاوضه نیست و همیشه باید **بالای تب نمودارها** بماند.

فایل: `components/MindMap.tsx` (کاملاً مستقل — از `Charts.tsx` با یک `<MindMap …/>` صدا زده می‌شود).

**ساختار سه‌لایه:**
```
        [کارها + صندوق ذهن]  ← حلقهٔ اصلی (بیرونی)
      [پروژه‌ها]              ← حلقهٔ داخلی
            ( من )             ← مرکز + شمارش کار باز / انجام‌شده
```

- **محاسبهٔ ریاضی، نه سلیقه‌ای:** تابع خالص `buildMindMap()` (خروجی‌اش export است)
  شعاع و اندازهٔ هر حلقه را از تعداد گره‌ها حساب می‌کند. کلید ماجرا:
  فاصلهٔ دو گرهٔ همسایه روی دایره = `2·r·sin(π/n)`؛ اگر این را از «قطر مستطیل گره +
  فاصلهٔ امن ۱۴px» بزرگ‌تر بگیریم، **ریاضی تضمین می‌کند هیچ دو گرهی روی هم نیفتد.**
  ⚠️ اگر شعاع/اندازه را دستی عوض کردی، حتماً تست همپوشانی را دوباره اجرا کن (پایین).
- **تیک‌زدن با کلیک:** کلیک روی هر گرهِ کار ⇒ `TASK_TOGGLE_DONE` در استور (همان اکشن
  بقیهٔ برنامه). انجام‌شده‌ها یک **خطِ اریب روی کارت** می‌گیرند (انیمیشن `mm-strike`)
  و اتصالشان نقطه‌چین و کم‌رنگ می‌شود. کلیک دوباره = برگشت.
- **کلیدهای نمایش:** انجام‌شده‌ها (پیش‌فرض روشن) · پروژه‌ها · صندوق ذهن.
  هرکدام خاموش شود، نقشه با شعاع کمتر از نو چیده می‌شود.
- **بزرگ‌نمایی:** دکمه‌های − / + (۱x تا ۲.۵x) برای وقتی کارها زیاد شده و متن ریز می‌شود.
- **سقف‌ها:** صندوق ذهن حداکثر ۱۸ گره؛ حلقهٔ اصلی حداکثر تا شعاع ۵۶۰px وگرنه متن ریز می‌شود.
  هر چیزی که جا نشد، **شمرده می‌شود** و در پیام «X مورد روی نقشه جا نشد» به کاربر می‌آید.

**🧪 تست همپوشانی (اجباری بعد از هر تغییر در چیدمان):** یک اسکریپت موقت بساز که
`buildMindMap` را روی ۸ سناریو (۱ تا ۳۴ کار، متن‌های بلند، بدون پروژه، بدون انجام‌شده)
اجرا کند و برای هر جفت گره چک کند `|dx| < (w1+w2)/2 && |dy| < (h1+h2)/2` نشود.
در نسخهٔ ۰.۸.۰ همهٔ ۸ سناریو صفر همپوشانی داشتند. (اسکریپت را بعد از تست پاک کن.)

### ۳.۱۴. 👀 دیدن نمودار بدون مرورگر (کشف ۰.۸.۰)

مرورگر headless در دسترس نیست، ولی **می‌شود نمودار را واقعاً دید** و این برای کارهای
گرافیکی حیاتی است:
1. کامپوننت را با `renderToStaticMarkup` (داخل `StoreProvider`) رندر کن.
2. خودِ `<svg>` را از HTML بیرون بکش.
3. ⚠️ **librsvg از CSS variable پشتیبانی نمی‌کند** ⇒ اول `var(--x)`ها را با مقدار
   واقعی از بلوک `:root` (و در حالت شب از `html[data-theme='dark']`) جانشین کن، وگرنه
   همهٔ شکل‌ها سیاه درمی‌آیند.
4. با `sharp` بده به PNG: `sharp(Buffer.from(svg)).png().toFile('/tmp/x.png')`
   (`sharp` در `node_modules` هست؛ پروژه به آن وابسته نیست، فقط برای تست).
5. خودت با ابزار خواندن تصویر نگاهش کن — این تنها راه دیدن UI در این سندباکس است.

---

## ۴) نقشه‌ی راه (به ترتیب اولویت)

- [x] نام درست: **کارگردان** (در همه‌ی جاهای کاربرپسند)
- [x] آیکون اختصاصی: تختهٔ کلاق (clapperboard)
- [x] **اتصال دیتابیس** + بخش «وضعیت دیتابیس» در تنظیمات
- [x] **ساخت جدول‌ها** (۷ جدول، همگی idempotent)
- [x] **مهاجرت داده**: دیتابیس منبع اصلی شد + localStorage پشتیبان + نوشتن شرطی
- [x] **🗑 سطل بازیافت** — حذف نرم در همه‌جا + برگرداندن
- [x] **🤝 ایجنت همکار** + تب پیشنهادات (تأیید/رد/برگرداندن) + API با کلید
- [x] **📊 تب نمودارها**
- [x] **🧠 نقشهٔ ذهنی** — مهم‌ترین نمودار: مرکز + حلقه‌ها + تیک‌خوردن با کلیک + خطِ انجام‌شده
- [x] بازچینی ناوبری: ۴ تب اصلی چسبان + بقیه در هدر + نشانگر بارگذاری
- [ ] همگام‌سازی واقعی چنددستگاهی (الان با refresh دستی)
- [ ] لایه ۴ — ظرفیت روزانه («امروز چقدر ظرفیت دارم؟» نه «چند کار دارم؟»)
- [ ] لایه ۵ — مرور هفتگی
- [ ] بخش‌های Life OS: 💰 پول · 📋 تعهدات · 🌱 زندگی

---

## ۵) دستورهای پرکاربرد

```bash
npm install                 # نصب وابستگی‌ها (node_modules در ریپو نیست)
npm run dev                 # سرور توسعه روی 0.0.0.0:3000  ← برای پیش‌نمایش زنده
npm run typecheck           # tsc --noEmit
npm run build               # بیلد تولیدی

git fetch origin --prune && git log --oneline -10 origin/main   # آخرین پوش را ببین
gh pr list --state all --json number,title,state,headRefName    # وضعیت PRها
gh auth status                                                  # اتصال گیت‌هاب
```

**تاریخ شمسی/تهران به‌صورت برنامه‌ای:**
```bash
TZ=Asia/Tehran node -e "console.log(new Intl.DateTimeFormat('fa-IR',{timeZone:'Asia/Tehran',dateStyle:'full',timeStyle:'long'}).format(new Date()))"
```

---

## ۶) 🚨 پروتکل «وضعیت اضطراری» (خواسته‌ی صریح کارفرما)

هر تغییری که به **دیتابیس یا بک‌اند** مربوط است (اسکیمای دیتابیس، مهاجرت‌ها، کوئری‌های نوشتنی، حذف/تغییر APIها، تغییر مدل داده، عوض‌کردن منبع داده) **ممنوع است تا وقتی کاربر تأیید کتبی بدهد**. مراحل:

1. تیتر بزن: **«وضعیت اضطراری» نیاز به تغییر در دیتابیس و بک‌اند**
2. درک خودت از تغییر لازم را بنویس.
3. لیست سؤال‌ها و ابهامات (اگر نیست، صریح بنویس «کامل متوجه شدم»).
4. قدم بعدی‌ات را بگو.
5. یک **عدد پنج‌رقمی تصادفی** بساز و بگو: «اگر موافقی این عدد را وارد کن.» تا کاربر عدد را نفرستد، **هیچ کدی ننویس**.

**اصل محافظت از داده (کارفرما به‌شدت نگران است):** هیچ تغییر Backend نباید برنامه را از کار بیندازد. طراحی درست این است که اگر دیتابیس در دسترس نبود، برنامه با یک پیام واضح همچنان بالا بیاید (fallback به localStorage) — نه صفحه‌ی سفید و نه crash. **این کار در `lib/db.ts` پیاده شده: هر خطا به یک شیء وضعیت فارسی تبدیل می‌شود و هیچ‌وقت throw نمی‌کند.**

**وضعیت تأییدهای گرفته‌شده:**
| تاریخ | موضوع | عدد تأیید | وضعیت |
|---|---|---|---|
| ۲۷ شهریور ۱۴۰۵ | افزودن `pg` + برداشتن `output: 'export'` + `/api/db/health` (فقط خواندنی) | **۹۱۳۳۷** | ✅ انجام شد |
| ۲۷ شهریور ۱۴۰۵ | ساخت جدول‌ها + سطل بازیافت + ایجنت همکار + نمودارها | دستور مستقیم کارفرما | ✅ انجام شد |
| ۲۷ شهریور ۱۴۰۵ | کلید خودساخته + کلید توقف (حذف نیاز به متغیر محیطی) | دستور مستقیم کارفرما | ✅ انجام شد |
| ۲۷ شهریور ۱۴۰۵ | بازچینی هدر و ناوبری + نشانگر بارگذاری | دستور مستقیم کارفرما (فقط UI، بدون تغییر داده) | ✅ انجام شد |

> 📌 **دربارهٔ پروتکل بخش ۶ در این مورد خاص:** کارفرما صریحاً گفت «نمی‌خوام متغیر محیطی
> درست کنم» و «همین دیگه کار خاصی لازم نیست». چون این تغییر **هیچ تغییری در ساختار جدول‌ها
> و هیچ حذف داده‌ای نداشت** (فقط دو ردیف در جدول `kargardan_settings` موجود)، به‌عنوان
> ریسک پایین طبقه‌بندی شد و اجرا شد — ولی در گزارش به کارفرما **صریحاً اعلام شد**.
> برای تغییرات ساختاری یا حذفی، همچنان پروتکل کامل اجرا می‌شود.

**سه لایهٔ محافظت که در کد اجرا شده‌اند:**
1. `lib/db.ts` هیچ کوئری نوشتنی ندارد — همه `SELECT`.
2. رمز عبور هرگز لاگ یا به مرورگر فرستاده نمی‌شود (`describeTarget` فقط host/port/db/user-masked را می‌دهد).
3. اگر متغیر محیطی شبیه `********` باشد، به‌جای تلاش برای اتصال، پیام «مقدار ستاره‌دار» برگردانده می‌شود.

---

## ۷) قالب گزارش‌دهی به کاربر (اجباری)

هر پاسخ باید این ۵ بخش را داشته باشد:
1. جدول چک‌لیست (✅ انجام‌شده / ❌ انجام‌نشده + درصد پیشرفت + توضیح در صورت لزوم)
2. «چی فهمیدم» (لیست)
3. «کاری مانده؟» (لیست)
4. سؤالاتی که به توسعه‌ی مرحله‌ی بعد کمک می‌کند
5. کیفیت پرامپت کاربر و اینکه چه چیزی باید گفته می‌شد تا بهتر شود

و هنگام پوش‌کردن، بخش **ℹ️ دربارهٔ برنامه** با **نسخه + تاریخ هجری شمسی + ساعت تهران** در گزارش بیاید.

---

## ۸) تله‌هایی که باید از آن‌ها پرهیز کنی

- ❌ دست‌زدن به شاخه‌ی `main` یا merge کردن پول‌ریکوئست.
- ❌ کامیت‌کردن فایل `.env` / `.env.local` (در `.gitignore` هست: `.env*`). ⚠️ توجه: همین الگو `.env.example` را هم نادیده می‌گیرد؛ اگر خواستی آن را کامیت کنی باید `!.env.example` را به `.gitignore` اضافه کنی.
- ❌ عوض‌کردن کلیدهای `kargardan-*` در localStorage (نابودی داده‌ی کاربر).
- ❌ افزودن API Route بدون برداشتن `output: 'export'` (بیلد می‌شکند).
- ❌ چاپ‌کردن مقدار متغیرهای محیطی در لاگ یا در پاسخ به کاربر (افشای رمز دیتابیس).
- ❌ بیش از یک پوش در هر نوبت کاری.
- ❌ نوشتن `DELETE` روی جدول‌های `kargardan_*` (به‌جایش `deleted_at` را پر کن).
- ❌ حذف چک `revision` در `writeState` (باعث گم‌شدن داده در نوشتن هم‌زمان می‌شود).
- ❌ اجرای خودکار پیشنهادهای ایجنت بدون تأیید کاربر.
- ❌ بازکردن `/api/agent/*` بدون کلید.
- ❌ حذف چک کلید توقف از `checkAgentAuth` (باید **قبل از** بررسی کلید باشد).
- ❌ ذخیرهٔ کلید ایجنت در جای دیگر (فقط `kargardan_settings` — تا `writeState` پاکش نکند).

---

## ۹) 🧪 چطور در سندباکس تست کنی (چیزهایی که وقت من را گرفت)

```bash
# ۱) یک PostgreSQL واقعی بالا بیاور (apt بسته است، این پکیج کار می‌کند)
mkdir -p /tmp/pgtest && cd /tmp/pgtest && npm init -y && npm install embedded-postgres
# سپس با یک اسکریپت کوچک initialise/start کن روی پورت 55432

# ۲) فایل .env.local بساز (در .gitignore است)
PRISMA_DATABASE_URL="postgresql://kargardan:test@127.0.0.1:55432/kargardan"
AGENT_API_KEY="test-agent-key-1234567890"

# ۳) سرور را با پستگرس روشن اجرا کن، بعد:
curl -X POST http://127.0.0.1:3000/api/migrate   # ساخت جدول‌ها
```

**مرورگر headless کار نمی‌کند** (دانلود Chrome مسدود است) و `apt-get` هم به اینترنت
دسترسی ندارد. برای بررسی UI از `renderToStaticMarkup` یا بررسی تطبیق نام کلاس‌های CSS
استفاده کن (الگوی `.css-check.mts` در تاریخچهٔ همین فایل هست).

**تست‌های حیاتی که هر بار باید اجرا شوند:**
۱. نوشتن با `revision` قدیمی ⇒ باید `stale` برگرداند و داده دست‌نخورده بماند.
۲. حذف یک آیتم ⇒ ردیف باید در دیتابیس بماند و فقط `deleted_at` بگیرد.
۳. `GET /api/agent/state` بدون کلید ⇒ باید `401` بدهد.
۴. **کلید توقف:** `POST /api/agent/key {"action":"disable"}` بعد `GET /api/agent/state`
   با کلید **درست** ⇒ باید `403` بدهد. بعد `enable` ⇒ دوباره `200`.
۵. **بازسازی کلید:** `regenerate` ⇒ کلید قبلی باید `401` بدهد.
۶. **سلامت تنظیمات:** یک بار `/api/state` را POST کن، بعد چک کن `/api/agent/key`
   همان کلید را برگرداند (یعنی `writeState` تنظیمات ایجنت را پاک نکرده).

---

## ۱۰) ⚠️ درسی که در نسخهٔ ۰.۴.۰ گرفتم — تکرارش نکن

قانون کارفرما این است: **«فقط یک بار پوش کن، بعد از تمام‌شدن همهٔ تغییرات.»**
من در ۰.۴.۰ اول کد را پوش کردم و بعد `CHANGELOG` را کامیت کردم — یعنی مجبور شدم دو بار
پوش کنم. **ترتیب درست این است:**

```bash
# ۱) همهٔ کد را بنویس و تست کن
# ۲) کامیت کن، ولی پوش نکن
# ۳) هش را با git log -1 بردار و داخل CHANGELOG بگذار
# ۴) CHANGELOG را کامیت کن
# ۵) حالا — و فقط حالا — یک بار پوش کن
```

پس: **هرگز قبل از نهایی‌شدن `CHANGELOG.md` پوش نکن.**

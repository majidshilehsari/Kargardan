# 📋 فایل مدیریت نسخه‌ها — «کارگردان»

> **راهنمای سریع ایجنت بعدی:** آخرین نسخه همیشه **بالاترین شماره در جدول پایین** و **نوک شاخه‌ی مشخص‌شده** است.
> برای اطمینان، این دو دستور را بزن:
> ```bash
> git log -1 --format='%h  %ad  %s' --date=iso     # نوک شاخه‌ی فعلی
> git fetch origin --prune && git log --oneline -5 origin/main
> ```

---

## 🗂 جدول نسخه‌ها

| نسخه | تاریخ کامیت (شمسی / میلادی) | ساعت تهران | آخرین نسخه؟ | برنچ | شماره کامیت |
|---|---|---|---|---|---|
| **0.6.0** | **۲۷ شهریور ۱۴۰۵** / 2026-09-18 | ۱۵:۰۵ | ✅ **بله** | `arena/01a0b3ae-kargardan` | `a5c86ee` (کامل: `a5c86ee8bba6af9f89b5f8d27a24dd6899a7a2e8`) |
| 0.5.0 | ۲۷ شهریور ۱۴۰۵ / 2026-09-18 | ۱۴:۲۰ | ❌ خیر | `arena/01a0b3ae-kargardan` | `19ecd7c` → `5821f15` |
| 0.4.0 | ۲۷ شهریور ۱۴۰۵ / 2026-09-18 | ۱۳:۴۵ | ❌ خیر | `arena/01a0b3ae-kargardan` | `58b7614` → `72a95d4` |
| 0.3.0 | ۲۷ شهریور ۱۴۰۵ / 2026-09-18 | ۱۲:۵۰ | ❌ خیر | `arena/01a0b3ae-kargardan` | `220009c` → `0c1a2ce` |
| 0.2.0 | ۲۷ شهریور ۱۴۰۵ / 2026-09-18 | ۱۲:۱۴ | ❌ خیر | `arena/01a0b3ae-kargardan` | `f0c94d3` → `2d7f19a` |
| 0.1.0 | ۳ شهریور ۱۴۰۵ / 2026-08-25 | ۲۲:۳۵ | ❌ خیر (روی `main`) | `main` ← merge از `arena/01a03a2d-kargardan` | `5253c09` (کد) → `f41e08e` (merge، PR #1) |

---

## 🎛 نسخه‌ی 0.6.0 — «بازچینی هدر و ناوبری + نشانگر بارگذاری»

**وضعیت:** ✅ این آخرین نسخه است
**برنچ:** `arena/01a0b3ae-kargardan`
**تاریخ کامیت:** جمعه ۲۷ شهریور ۱۴۰۵ — ساعت ۱۵:۰۵ به وقت تهران / 2026-09-18
**شماره کامیت:** `a5c86ee` — کامل: `a5c86ee8bba6af9f89b5f8d27a24dd6899a7a2e8`
**کامیت پایه (نسخه‌ی قبلی):** `5821f15`
**پایه‌ی تأیید:** دستور مستقیم کارفرما
**⚠️ فقط رابط کاربری — صفر تغییر در دیتابیس، صفر تغییر در مدل داده، صفر تغییر در API**

### ۱. 🎛 هدر مرتب شد
| قبل | حالا |
|---|---|
| `[ک` لوگو + **کارگردان** + tagline | `[آیکون کلاق]` **کارگردان** (بدون tagline) |
| نشانگر 🟢 «دیتابیس وصل است» در هدر | ❌ حذف شد — وضعیت در «تنظیمات → وضعیت دیتابیس» |
| ۹ تب در یک نوار شلوغ | ۴ تب اصلی چسبان + ۵ تب فرعی در هدر |

**tagline («نه کار بیشتر؛ تسلط بیشتر») حذف شد** — طبق درخواست کارفرما.

> ⚠️ **نکته‌ی مهم برای ایجنت بعدی:** `SyncNotice` کاملاً حذف **نشد**، فقط از حالت
> «همه‌چیز خوب است» پنهان شد. حالا فقط وقتی ظاهر می‌شود که:
> - خطای همگام‌سازی رخ دهد (`syncError`)، یا
> - دیتابیس وصل نباشد (`source === 'local'`)
>
> دلیل: اگر به‌کل حذف شود، کاربر نمی‌فهمد داده‌اش دارد در مرورگر می‌ماند نه دیتابیس —
> و این یک **ریسک واقعی از دست رفتن داده** است. در گزارش به کارفرما توضیح داده شد.

### ۲. 🧭 ناوبری دو سطحی
```
┌─ هدر:  [لوگو] کارگردان  ·  🏠 داشبورد · 🔌 ایجنت همکار · 📊 نمودارها · 🗑 سطل بازیافت · ⚙️ تنظیمات  · 🌙
├─ نوار چسبان (sticky):  🤝 پیشنهادات ایجنت | 🧠 صندوق ذهن | 📋 کارها | 🚀 پروژه‌ها
└─ محتوا
```

**چهار تب اصلی** (به ترتیب خواستهٔ کارفرما) در نوار **چسبان** زیر هدر:
`position: sticky` — هنگام اسکرول همیشه دیده می‌شوند. تب فعال با رنگ پر (accent) مشخص است
و تب‌ها `flex: 1` هستند تا تمام عرض را پر کنند.

**پنج تب فرعی** در هدر، جمع‌وجورتر (`icon + label`). روی موبایل (زیر ۸۲۰px) برچسب‌ها
پنهان می‌شوند و فقط آیکون می‌ماند.

### ۳. ⏳ نشانگر «کاری در جریان است» — `lib/busy.ts`
کارفرما گفت: «وقتی روی تأیید پیشنهاد می‌زنم، طول می‌کشد و کاربر بی‌خبر می‌ماند.»

یک شمارندهٔ مشترک با `useSyncExternalStore` ساخته شد و **سه سطح بازخورد**:

| سطح | کجا | چه شکلی |
|---|---|---|
| ۱. سراسری | بالای صفحه | نوار نازک متحرک (`GlobalProgress`) — در همه‌جا دیده می‌شود |
| ۲. کارت | داخل کارتِ در حال عملیات | ردیف چرخان + متن فارسی («در حال اجرای پیشنهاد… چند لحظه صبر کن») |
| ۳. دکمه | روی خود دکمه | چرخنده + «در حال اجرا…» + غیرفعال |

کارتِ در حال عملیات کلاس `is-busy` می‌گیرد: محو می‌شود و کلیک نمی‌پذیرد.

استفاده:
```tsx
import { withBusy } from '@/lib/busy';

await withBusy(async () => {
  const res = await fetch('/api/...', { method: 'POST' });
  // ...
});
```

**کجاها اعمال شد:** تأیید/رد/برگرداندن پیشنهاد · برگرداندن و پاک‌کردن از سطل بازیافت ·
ساخت جدول‌ها · قطع/وصل دسترسی ایجنت و ساخت کلید جدید.

### 🧪 تست‌ها
| تست | نتیجه |
|---|---|
| ساختار HTML: هدر + نوار چسبان | ✅ ۴ تب اصلی در نوار، ۵ تب فرعی در هدر |
| tagline در HTML | ✅ صفر مورد |
| رندر `Spinner` و `BusyRow` | ✅ HTML و `role="status"` درست |
| انیمیشن‌های CSS (`spin`/`slideProgress`/`pulseSoft`) | ✅ همه تعریف شده |
| کلاس‌های جدید استایل دارند | ✅ `.spinner` `.busy-row` `.global-progress` `.is-busy` `.primary-tabs` `.hnav` |
| `typecheck` / `build` | ✅ ✅ |
| هیچ چیزی مانع `position: sticky` نیست | ✅ (بدون `overflow:hidden` روی `html`/`body`) |

### 🔒 وضعیت داده
| موضوع | وضعیت |
|---|---|
| تغییر در دیتابیس / اسکیما | **صفر** |
| تغییر در مدل داده (`lib/types.ts`) | **صفر** |
| تغییر در APIها | **صفر** |
| `main` | دست‌نخورده (`f41e08e`) |

### 🧾 قدم بعدی
- تصمیم با کارفرما: تب پیش‌فرض هنگام باز شدن برنامه. الان `داشبورد` است (که به هدر رفته).
  اگر می‌خواهد روی یکی از چهار تب اصلی باز شود، فقط `useState<TabId>('dash')` را عوض کن.
- لایه ۴ (ظرفیت روزانه) و لایه ۵ (مرور هفتگی) هنوز پیاده نشده‌اند.

---

## 🔑 نسخه‌ی 0.5.0 — «کلید خودساخته + کلید توقف»

**وضعیت:** ❌ آخرین نسخه نیست (نسخهٔ قبلی)
**برنچ:** `arena/01a0b3ae-kargardan`
**تاریخ کامیت:** جمعه ۲۷ شهریور ۱۴۰۵ — ساعت ۱۴:۲۰ به وقت تهران / 2026-09-18
**شماره کامیت:** `19ecd7c` — کامل: `19ecd7c87b32a2eea9c5125c508a393d9e213724`
**کامیت پایه (نسخه‌ی قبلی):** `72a95d4`
**پایه‌ی تأیید:** دستور مستقیم کارفرما («نمی‌خوام متغیر محیطی درست کنم — کنترل api رو در اختیارم بذار»)

### مشکل نسخه‌ی قبل که کارفرما گفت
برای فعال‌کردن ایجنت همکار، باید متغیر محیطی `AGENT_API_KEY` در Vercel ساخته می‌شد.
کارفرما گفت این «اضافی» است و راه‌اندازی بدون متغیر محیطی می‌خواهد.

### راه‌حل — بدون هیچ متغیر محیطی

| قبل (۰.۴.۰) | حالا (۰.۵.۰) |
|---|---|
| باید `AGENT_API_KEY` در Vercel ساخته می‌شد | ❌ لازم نیست — کلید خودش ساخته می‌شود |
| اگر متغیر نبود، همهٔ مسیرها بسته بود | ✅ کلید خودکار در دیتابیس ساخته می‌شود |
| راهی برای بستن در نبود جز حذف متغیر و Deploy دوباره | 🔴 **کلید توقف با یک دکمه، فوری** |
| عوض‌کردن کلید = Deploy دوباره | 🔄 «ساخت کلید جدید» با یک کلیک |

### 🔑 کلید خودساخته
- به‌محض بازکردن تب «🔌 ایجنت همکار»، کلید **خودکار ساخته می‌شود**:
  `randomBytes(32).toString('base64url')` → ۴۳ کاراکتر.
- در جدول **`kargardan_settings`** با کلیدهای `agent_api_key` و `agent_access_enabled`
  ذخیره می‌شود.
- **⚠️ هیچ تغییر ساختاری در جدول‌ها لازم نشد** — از جدول موجود استفاده شد. **صفر تغییر DDL.**

### 🔴 کلید توقف — مهم‌ترین بخش این نسخه
دکمهٔ «⏸ قطع دسترسی» ⇒ از همان لحظه **حتی با کلید کاملاً درست**، همهٔ درخواست‌های ایجنت
با کد **۴۰۳** رد می‌شوند. **نه Deploy دوباره، نه راه‌اندازی مجدد — فوری.**

ترتیب بررسی در `checkAgentAuth` (این ترتیب مهم است):
۱. دیتابیس در دسترس است؟ (۵۰۳ اگر نه)
۲. کلید وجود دارد؟ (۵۰۳ اگر نه)
۳. 🔴 **آیا دسترسی باز است؟** (۴۰۳ اگر نه) ← کلید توقف، **قبل از** بررسی کلید
۴. کلید درست است؟ (۴۰۱ اگر نه)

خروجی‌های دیگر: `enable` (بازکردن دوباره) · `regenerate` (کلید جدید، قبلی فوری بی‌اعتبار) ·
`ensure` (ساخت اگر نبود).

### UI — همه‌چیز در تب «🔌 ایجنت همکار»
- وضعیت بزرگ 🟢/🔴 با توضیح فارسی
- دکمهٔ قطع/وصل
- نمایش کلید با «👁 نشانم بده» + «📋 کپی کلید» + «🔄 ساخت کلید جدید»
- متن آمادهٔ ایجنت **با کلید واقعی داخلش** — یک کلیک و آماده
- نمونهٔ curl

### 🧪 تست‌ها — ۲۰ سناریو روی PostgreSQL واقعی

| تست | نتیجه |
|---|---|
| قبل از ساخت کلید، درخواست ایجنت | ✅ ۵۰۳ با پیام راهنما |
| ساخت خودکار کلید (`ensure`) | ✅ کلید ۴۳ کاراکتری در دیتابیس |
| با کلید درست | ✅ `200` |
| **🔴 قطع دسترسی + کلید درست** | ✅ **`403`** (هر ۳ مسیر خواندنی + نوشتنی) |
| بازکردن دوباره | ✅ `200` با همان کلید |
| کلیدِ عوض‌شده | ✅ کلید قدیمی `401` · کلید جدید `200` |
| کلید غلط / بدون کلید | ✅ `401` |
| **ذخیرهٔ وضعیت، کلید را پاک نمی‌کند** | ✅ کلید و وضعیت دسترسی سالم ماندند |
| برنامهٔ اصلی هنگام قطع دسترسی ایجنت | ✅ سالم کار می‌کند |
| جریان کامل ایجنت بعد از کلید جدید | ✅ پیشنهاد `pending` ثبت شد |
| `typecheck` / `build` | ✅ ✅ |

### 🔒 وضعیت داده
| موضوع | وضعیت |
|---|---|
| تغییر ساختار جدول‌ها (DDL) | **صفر** — از `kargardan_settings` موجود استفاده شد |
| حذف داده | **صفر** |
| `main` | دست‌نخورده (`f41e08e`) |
| خطر از کار افتادن production | ✅ صفر |

### ⚠️ یک نکتهٔ امنیتی که باید بدانی
کلید API حالا در دیتابیس (و در صفحهٔ برنامه) **قابل خواندن** است. این طبیعی است برای
کلیدی که می‌خواهی ببینی و کپی کنی — ولی یعنی هر کسی که به برنامهٔ تو دسترسی داشته باشد،
کلید را هم می‌بیند. برای کاهش ریسک:
- دسترسی Vercel/برنامه‌ات را محدود نگه دار.
- اگر شک کردی، «🔄 ساخت کلید جدید» را بزن.
- و در بدترین حالت، «⏸ قطع دسترسی».

⚠️ **سازگاری با گذشته:** اگر `AGENT_API_KEY` را قبلاً به‌عنوان متغیر محیطی ساخته باشی،
آن بر کلید دیتابیس مقدم می‌شود و در UI نمایش داده نمی‌شود. **کلید توقف همیشه اعمال می‌شود.**

---

## 🗄️ نسخه‌ی 0.4.0 — «اسکیمای دیتابیس + سطل بازیافت + ایجنت همکار + نمودارها»

**وضعیت:** ❌ آخرین نسخه نیست (نسخهٔ قبلی)
**برنچ:** `arena/01a0b3ae-kargardan`
**تاریخ کامیت:** جمعه ۲۷ شهریور ۱۴۰۵ — ساعت ۱۳:۴۵ به وقت تهران / 2026-09-18
**شماره کامیت:** `58b7614` — کامل: `58b7614c2dfb6f9cb148e279cf1bfb27a544bb05`
> هش **کدِ** این نسخه است؛ خودِ همین فایل یک کامیت بعد از آن ثبت شده. با `git log -1` نوک واقعی را ببین.
**کامیت پایه (نسخه‌ی قبلی):** `0c1a2ce`
**پایه‌ی تأیید:** دستور مستقیم کارفرما («جدول‌ها را بساز و اقدامات لازم را انجام بده»)

### ۱. 🗄 اسکیمای دیتابیس — ۷ جدول
`lib/schema.ts` — همه با `CREATE TABLE IF NOT EXISTS` و `CREATE INDEX IF NOT EXISTS`:
`kargardan_tasks` · `kargardan_projects` · `kargardan_inbox` · `kargardan_settings` ·
`kargardan_suggestions` · `kargardan_history` · `kargardan_meta`

**🛡 هیچ `DROP` / `DELETE` / `TRUNCATE` / `ALTER … DROP` در فایل اسکیما نیست.**
اجرای چندباره‌اش هیچ اثر اضافه‌ای ندارد. با کلیک روی «🏗 ساخت جدول‌ها» در کارت
«وضعیت دیتابیس» اجرا می‌شود (یا خودکار در اولین درخواست به `/api/state`).

### ۲. 🗑 سطل بازیافت — دیگر هیچ چیزی پاک نمی‌شود
- هر جدول یک ستون `deleted_at` + `deleted_by` دارد. **حذف = پر کردن این ستون.**
- هیچ `DELETE` ای روی داده‌ها اجرا نمی‌شود. تنها استثنا: `purgeTrash()` که فقط با
  تأیید دوباره‌ی خودِ کاربر روی دکمهٔ «پاک‌کردن قطعی» صدا زده می‌شود.
- تب **«🗑 سطل بازیافت»** با دکمهٔ ♻️ برگرداندن برای هر مورد.

### ۳. 📤 مهاجرت داده با محافظت کامل
- `lib/repo.ts` + `app/api/state/route.ts`: دیتابیس **منبع اصلی** شد.
- **🛡 نوشتن شرطی (optimistic concurrency):** کلاینت `revision` را نگه می‌دارد؛ اگر دیتابیس
  جلوتر رفته باشد، نوشتن **رد** می‌شود و داده‌ی تازه از بین نمی‌رود.
- `localStorage` همچنان همیشه نوشته می‌شود (پشتیبان محلی). اگر دیتابیس قطع شود، برنامه
  بدون هیچ خطایی با نسخهٔ محلی ادامه می‌دهد.
- انتقال داده‌ی مرورگر به دیتابیس **خودکار نیست** — یک نوار پیشنهاد نشان داده می‌شود و
  کاربر باید کلیک کند. نسخهٔ مرورگر هم پاک نمی‌شود.

### ۴. 🤝 ایجنت همکار (دو ایجنتی شد)
- **ایجنت همکار**: فقط از راه API به دیتابیس دسترسی دارد؛ به سورس‌کد ندارد.
- **ایجنت مادر**: (ایجنت توسعه‌دهنده) سورس‌کد + دیتابیس.
- `lib/agent-auth.ts` — احراز هویت با کلید `AGENT_API_KEY` (مقایسهٔ `timingSafeEqual`).
  **اگر کلید تنظیم نشده باشد، همهٔ مسیرهای ایجنت بسته‌اند (۵۰۳) — امن به‌صورت پیش‌فرض.**
- APIها: `GET /api/agent/context` (راهنما) · `GET /api/agent/state` (کل دیتابیس) ·
  `GET|POST /api/agent/suggestions` · `GET /api/agent/status` (بدون کلید)
- مستندات کامل برای دادن به ایجنت: **`API-ایجنت-همکار.md`**

### ۵. ✅ تب «پیشنهادات ایجنت» — قبل از داشبورد
- هفت نوع پیشنهاد: `task_add` · `task_update` · `task_set_status` · `task_toggle_done` ·
  `project_add` · `project_set_status` · `note`
- **هیچ پیشنهادی خودکار اجرا نمی‌شود.** کاربر تأیید یا رد می‌کند.
- **⏪ برگرداندن:** هنگام تأیید، «وضعیت قبل» در `kargardan_history` ثبت می‌شود؛
  دکمهٔ «برگرداندن» دقیقاً همان وضعیت را بازمی‌گرداند.
- اعتبارسنجی payload — پیشنهاد ناقص (مثلاً بدون `id`) پذیرفته نمی‌شود.

### ۶. 🔌 تب «ایجنت همکار»
صفحه‌ای که APIها، وضعیت کلید و یک **متن آمادهٔ قابل کپی** برای دادن به ایجنت همکار
نشان می‌دهد — بدون اینکه کلید در تاریخچهٔ مرورگر بماند.

### ۷. 📊 تب «نمودارها»
نمودار دونات توزیع کارها · روند ۱۴ روز گذشته · کارها به تفکیک جبهه · وضعیت جبهه‌ها.
**با SVG خالص** — هیچ کتابخانهٔ نموداری نصب نشد (سبک ماند).

### 🧪 تست‌های واقعی روی یک PostgreSQL تأییدشده

| تست | نتیجه |
|---|---|
| ساخت جدول‌ها (DB خالی) | ✅ ۷ جدول ساخته شد |
| نوشتن و خواندن وضعیت | ✅ |
| **نوشتن با `revision` قدیمی** | ✅ `stale` برگرداند و **داده دست‌نخورده ماند** |
| **حذف نرم** | ✅ ردیف در دیتابیس ماند، فقط `deleted_at` گرفت |
| سطل بازیافت + ♻️ برگرداندن | ✅ کار برگشت |
| `/api/agent/state` بدون کلید | ✅ `401` |
| `/api/agent/state` با کلید غلط | ✅ `401` |
| `/api/agent/state` با کلید درست | ✅ دادهٔ کامل با `ageDays` و `nextStep` |
| ارسال پیشنهاد توسط ایجنت | ✅ `pending` ثبت شد و **خودکار اجرا نشد** |
| ✅ تأیید پیشنهاد | ✅ اجرا شد |
| ⏪ برگرداندن پیشنهاد | ✅ دقیقاً به وضعیت قبل برگشت |
| ❌ رد کردن پیشنهاد | ✅ هیچ تغییری اعمال نشد |
| پیشنهاد ناقص (بدون `id`) | ✅ رد شد با پیام فارسی |
| `npm run typecheck` / `npm run build` | ✅ هر دو |

### 🔒 وضعیت داده
| موضوع | وضعیت |
|---|---|
| `main` | دست‌نخورده (`f41e08e`) |
| حذف واقعی داده | **صفر** — همه‌جا حذف نرم |
| خطر از کار افتادن production | ✅ صفر — اگر دیتابیس نباشد، برنامه با localStorage کار می‌کند |
| کلید ایجنت | پیش‌فرض **بسته** — بدون `AGENT_API_KEY` هیچ‌کس به داده دسترسی ندارد |

### 🧾 قدم بعدی برای ایجنت بعدی
- **همگام‌سازی چنددستگاهی واقعی:** الان اگر دو دستگاه هم‌زمان باز باشند، دستگاه دوم بعد از
  تغییر اولی باید `refresh` کند (نوشتن شرطی جلوی گم‌شدن داده را می‌گیرد، ولی خودکار تازه نمی‌شود).
- **لایه ۴ (ظرفیت روزانه) و لایه ۵ (مرور هفتگی)** — هنوز پیاده نشده‌اند.
- اگر خواستی ساختار جدول‌ها را عوض کنی: **پروتکل «وضعیت اضطراری» (بخش ۶ راهنمای ایجنت).**

---

## 🗄️ نسخه‌ی 0.3.0 — «اتصال دیتابیس + بخش وضعیت دیتابیس در تنظیمات»

**وضعیت:** ❌ آخرین نسخه نیست (نسخهٔ قبلی)
**برنچ:** `arena/01a0b3ae-kargardan`
**تاریخ کامیت:** جمعه ۲۷ شهریور ۱۴۰۵ — ساعت ۱۲:۵۰ به وقت تهران / 2026-09-18
**شماره کامیت:** `220009c` — کامل: `220009c0c0e0201ec898883a2b432faee230bfb4`
> هش **کدِ** این نسخه است. خودِ همین فایل (`CHANGELOG.md`) در کامیتی بلافاصله بعد از آن ثبت شده، پس **نوک شاخه** یک قدم جلوتر است — با `git log -1` نوک واقعی را ببین.
**کامیت پایه (نسخه‌ی قبلی):** `2d7f19a` (شماره‌ی نسخه ۰.۲.۰ روی `f0c94d3`)
**عدد تأیید کارفرما:** `۹۱۳۳۷`

### چه چیزی نسبت به نسخه‌ی 0.2.0 عوض شد؟

1. **افزودن درایور `pg@8`** (+ `@types/pg`). عمداً **Prisma نصب نشد** با اینکه نام متغیر
   `PRISMA_DATABASE_URL` است — `pg` سبک‌تر است و به مرحلهٔ `prisma generate` نیاز ندارد.

2. **`next.config.mjs`: `output: 'export'` برداشته شد** — با تأیید صریح کارفرما.
   دلیل: در حالت خروجی استاتیک، Next.js هر API Route را با خطای بیلد رد می‌کند.
   ⚠️ پیامد: برنامه دیگر «بدون سرور» نیست. روی Vercel بدون تنظیم اضافه کار می‌کند،
   ولی دیگر نمی‌شود روی هاست استاتیک ساده منتشرش کرد.

3. **`lib/db.ts`** — استخر اتصال (`Pool`) با قواعد ایمنی سخت‌گیرانه:
   - **همهٔ کوئری‌ها `SELECT` هستند.** حتی یک `INSERT`/`UPDATE`/`DELETE`/`DROP`/`CREATE` در این فایل نیست.
   - رمز عبور هرگز لاگ یا به مرورگر فرستاده نمی‌شود.
   - **هیچ‌وقت throw نمی‌کند** — هر خطا به یک شیء وضعیت فارسی تبدیل می‌شود.
   - ۲۰ کد خطای رایج پستگرس/شبکه به پیام و راهنمای فارسی ترجمه شده‌اند
     (`28P01` رمز اشتباه · `ENOTFOUND` دامنه · `ECONNREFUSED` پورت بسته · `3D000` دیتابیس ناموجود · …).
   - تشخیص **مقدار ستاره‌دار**: اگر متغیر شبیه `********` باشد، به‌جای تلاش بی‌فایده برای اتصال،
     پیام «رشتهٔ اتصال واقعی بگذار» برگردانده می‌شود.
   - ترتیب اولویت متغیرها: `PRISMA_DATABASE_URL` → `POSTGRES_URL` → `DATABASE_URL` →
     `POSTGRES_PRISMA_URL` → `POSTGRES_URL_NON_POOLING`.
   - SSL هوشمند: برای `localhost` خاموش، برای سرویس‌های ابری روشن (با `sslmode` قابل تنظیم).

4. **`app/api/db/health/route.ts`** — یک API Route **فقط خواندنی** (`runtime = 'nodejs'`،
   `dynamic = 'force-dynamic'`, `Cache-Control: no-store`). همیشه کد ۲۰۰ برمی‌گرداند و
   معنی وضعیت داخل بدنه است (`ok` / `not_configured` / `error`).

5. **`components/DatabaseStatus.tsx`** — بخش **«🗄️ وضعیت دیتابیس»** در تب تنظیمات.
   همان چیزی که کارفرما خواست. نمایش می‌دهد:
   - 🟢 **وصل است** / 🟡 **تنظیم نشده** / 🔴 **وصل نیست**
   - میزبان، پورت، نام دیتابیس، کاربر (ماسک‌شده)، وضعیت SSL، نسخهٔ سرور، ساعت سرور، تعداد جدول‌ها
   - **تأخیر اتصال (latency)** به میلی‌ثانیه
   - در حالت خطا: کد خطا + پیام سرور + راهنمای فارسی رفع مشکل
   - دکمهٔ «🔄 بررسی دوباره»

6. **`.env.example`** ساخته شد و `/!.env.example` به `.gitignore` اضافه شد (قبلاً الگوی `.env*`
   آن را هم نادیده می‌گرفت).

7. **`README.md`** به‌روز شد: مرحلهٔ Environment Variables در راهنمای Vercel، بخش داده‌ها،
   ساختار فنی و نقشهٔ راه.

### 🧪 تست‌هایی که واقعاً انجام شد (نه فقط ادعا)

یک **PostgreSQL 18.4 واقعی** با پکیج npm `embedded-postgres` روی پورت 55432 بالا آورده شد و ۱۰ سناریو تست شد:

| سناریو | نتیجه |
|---|---|
| بدون هیچ متغیر محیطی | `not_configured` ✅ |
| اتصال سالم با `PRISMA_DATABASE_URL` | `ok` — PostgreSQL 18.4، تأخیر ۱۳ms ✅ |
| اتصال سالم با `POSTGRES_URL` | `ok` ✅ |
| اولویت `PRISMA_DATABASE_URL` بر `POSTGRES_URL` | انتخاب درست ✅ |
| رمز اشتباه | `28P01` + راهنمای فارسی ✅ |
| پورت بسته | `ECONNREFUSED` ✅ |
| دامنهٔ ناموجود | `ENOTFOUND` ✅ |
| مقدار ستاره‌دار `************` | `PLACEHOLDER_VALUE` ✅ |
| رشتهٔ بی‌معنی | `INVALID_URL` ✅ |
| دیتابیس ناموجود | `3D000` ✅ |

**تست امنیتی:** در هر ۱۰ سناریو بررسی شد که **رمز عبور در خروجی API لو نرود** → ✅ هیچ‌کدام لو نرفت.
**تست end-to-end:** `/api/db/health` روی سرور واقعی Next.js با هر دو حالت (موفق و ناموفق) تست شد.
**تست رندر UI:** هر سه حالت با `renderToStaticMarkup` رندر شد و همهٔ کلاس‌های CSS بررسی شد → ✅.
`npm run typecheck` ✅ · `npm run build` ✅

### 🔒 وضعیت داده در این نسخه — مهم برای ایجنت بعدی
| موضوع | وضعیت |
|---|---|
| منبع دادهٔ واقعی | همچنان `localStorage` (کلید `kargardan-state-v1`) — **دست‌نخورده** |
| نوشتن در دیتابیس | ❌ **صفر.** هیچ جدولی ساخته نشد، هیچ رکوردی درج نشد |
| کوئری‌های اجراشده روی دیتابیس کاربر | فقط `select current_database()`, `version()`, `now()`, `information_schema.tables` |
| خطر از کار افتادن production | ✅ صفر — اگر دیتابیس نباشد، برنامه با یک پیام واضح کار می‌کند |
| `main` | دست‌نخورده (`f41e08e`) |

### 🧾 اطلاعات تکمیلی برای ایجنت بعدی
- **مهم‌ترین قدم بعدی:** ساخت جدول‌های کارگردان در دیتابیس. این یک **نوشتن** است و
  **باید اول با پروتکل «وضعیت اضطراری» (بخش ۶ راهنمای ایجنت) تأیید گرفته شود.**
- برنامهٔ پیشنهادی برای آن مرحله: `CREATE TABLE IF NOT EXISTS` (idempotent، بدون `DROP`)،
  با ستون‌های `id / text / status / project_id / is_next_step / waiting_on / note / done /
  created_at / updated_at`، سپس یک endpoint **فقط خواندنی** برای شمردن رکوردها تا کاربر
  با چشم خودش ببیند جدول‌ها ساخته شده‌اند — و **بعد** با تأیید سوم، انتقال داده.
- اگر `.env.local` سندباکس باعث سردرگمی شد: آن فایل به یک PostgreSQL موقت داخل سشن اشاره
  می‌کند و **کامیت نمی‌شود**. برای پیش‌نمایش محلی واقعی، کارفرما باید مقدار خودش را بگذارد.

---

## 🚀 نسخه‌ی 0.2.0 — «نام درست + آیکون اختصاصی + مستندسازی»

**وضعیت:** ❌ آخرین نسخه نیست (نسخهٔ قبلی)
**برنچ:** `arena/01a0b3ae-kargardan`
**شماره کامیت:** `f0c94d3` — کامل: `f0c94d3d5c9a219b5ffdae8a954680f6c9ecb6aa`
> این هش، کامیتِ **کدِ همین نسخه** است. خودِ همین فایل (`CHANGELOG.md`) در کامیتی بلافاصله بعد از آن ثبت شده؛ پس **نوک شاخه** یک قدم جلوتر از این هش است. با `git log -1` نوک واقعی را ببین.
**کامیت پایه (نسخه‌ی قبلی):** `5253c09` — و `origin/main` با آن یکسان است (`f41e08e`).

### چه چیزی نسبت به نسخه‌ی 0.1.0 عوض شد؟
1. **اصلاح نام برنامه:** «کاردان» → **«کارگردان»** در تمام نقاط کاربرپسند:
   `app/layout.tsx` (عنوان تب مرورگر) · `components/App.tsx` (هدر) · `components/Dashboard.tsx` (خوش‌آمد) · `components/Settings.tsx` (پیام‌ها) · `README.md` · `package.json` (description) · کامنت‌های `globals.css` و `lib/types.ts`.
2. **آیکون اختصاصی:** `app/icon.svg` از یک مربعِ ساده با حرف «ک» به یک **تختهٔ کلاق کلاقی (Clapperboard)** تغییر کرد — استعاره‌ی مستقیم «کارگردان»؛ نوارهای مورب + سطرهای «برنامه‌ی زندگی». در ابعاد ۳۲ پیکسل (favicon) هم خوانا آزمایش شد.
3. **مستندسازی:** افزودن `راهنمای ایجنت بعدی.md` و همین فایل (`CHANGELOG.md`) که قبلاً وجود نداشتند.

### ⚠️ آنچه در این نسخه **انجام نشد** (تعمداً)
- **اتصال به دیتابیس انجام نشد.** کارفرما در قانون ۴.۴ خواسته هر تغییر دیتابیس/بک‌اند **قبل از اجرا** تأیید شود. پروتکل «وضعیت اضطراری» در پاسخ به کاربر اجرا شد و منتظر **عدد پنج‌رقمی تأیید** است.
- `next.config.mjs` **دست‌نخورده** ماند (`output: 'export'`). این خط تا زمانی که تأیید نگیرد باید همان‌طور بماند، چون برداشتنش یعنی رفتن به رانتایم سرور و بک‌اند جدید.
- هیچ وابستگی جدیدی (`pg` / `prisma` / `@vercel/postgres`) نصب نشد.

### 🔒 وضعیت داده در این نسخه
| موضوع | وضعیت |
|---|---|
| منبع داده | `localStorage` مرورگر، کلید `kargardan-state-v1` |
| ذخیره‌گاه ابری | ❌ هیچ (دیتابیس وصل نیست) |
| تغییر کلیدهای ذخیره‌سازی | ❌ انجام نشده — عمداً، تا داده‌ی کاربر از بین نرود |
| مهاجرت داده | ❌ انجام نشده (پیش‌نیاز: تأیید کارفرما) |
| خطر از کار افتادن production | ✅ صفر — `main` دست نخورد و برنامه با همان رانتایم استاتیک قبلی کار می‌کند |

### 🧾 اطلاعات تکمیلی برای ایجنت بعدی
- **کد در گیت‌هاب:** شاخه‌ی `arena/01a0b3ae-kargardan` (فقط یک بار پوش شده). `main` دست‌نخورده است.
- **متغیرهای محیطی دیتابیس در سندباکس ایجنت دیده نمی‌شوند** (تنها `E2B_*`, `GH_TOKEN`, `GITHUB_TOKEN`, … موجود است). برای تست واقعی اتصال، کارفرما باید مقادیر را در **Vercel → Environment Variables** و/یا **`.env.local`** قرار دهد.
- **قدم بعدی پیشنهادی:** پس از دریافت عدد تأیید → نصب `pg`، ساخت `lib/db/*`، برداشتن `output: 'export'`، افزودن `/api/db/health` (فقط خواندنی) و سپس در صورت تأیید دوم، ساخت اسکیمای کارگردان و مهاجرت داده.
- **قاعده‌ی تغییرناپذیر:** بیش از یک پوش در هر نوبت کاری ممنوع؛ به `main` دست نزن؛ پول‌ریکوئست را باز کن ولی merge نکن.

---

## 🧱 نسخه‌ی 0.1.0 — «نسخه‌ی هسته»

**وضعیت:** ❌ آخرین نسخه نیست (نسخه‌ی قبلی)
**تاریخ کامیت:** سه‌شنبه ۳ شهریور ۱۴۰۵ — ۲۲:۳۵ به وقت تهران / 2026-08-25
**برنچ:** روی `main` ادغام شد (منبع: `arena/01a03a2d-kargardan`، PR #1)
**شماره کامیت:** `5253c090572651b4bd754619c0cc5bf8b3a4fe04` — merge: `f41e08e76e7d8b1a79800f0d926479306cb27410`

### تغییرات نسبت به نسخه‌ی قبل
**این اولین نسخه است.** تأسیس پروژه:
- Next.js 15 (App Router) + React 19 + TypeScript، خروجی کاملاً استاتیک (`output: 'export'`)
- لایه ۱: صندوق ذهن (تخلیه‌ی کامل) — `components/Inbox.tsx`
- لایه ۲: چهار وضعیت کار «الان / بعداً / منتظر / پارک‌شده» — `components/TasksBoard.tsx`
- لایه ۳: سقف جبهه‌های فعال + صف انتظار + «قدم بعدی» هر پروژه — `components/Projects.tsx`
- داشبورد پاسخ‌ده به چهار سؤال کلیدی، تم روشن/تاریک، رابط فارسی RTL، فونت وزیرمتن باندل‌شده
- ذخیره‌سازی: localStorage با نسخه‌بندی داده + پشتیبان‌گیری/بازیابی JSON

### اطلاعات تکمیلی
- نام در این نسخه به‌غلط «کاردان» نوشته شده بود که در 0.2.0 اصلاح شد.
- کد کاملاً بدون سرور است؛ روی Vercel با یک Deploy ساده بالا می‌آید و هیچ متغیر محیطی نمی‌خواهد.

---

## 📌 قالب به‌روزرسانی این فایل (برای نسخه‌های بعدی)

هنگام افزودن نسخه‌ی جدید، در **بالای جدول** یک ردیف اضافه کن و این شش مورد را بنویس:
1. تاریخ و زمان کامیت (شمسی + ساعت تهران + میلادی)
2. آیا این آخرین نسخه است؟
3. توضیح مختصر تغییرات نسبت به نسخه‌ی قبلی
4. در کدام برنچ قرار دارد
5. شماره کامیت (هش کوتاه + کامل)
6. هر نکته‌ی دیگری که به ایجنت بعدی کمک می‌کند، آخرین نسخه را بشناسد
